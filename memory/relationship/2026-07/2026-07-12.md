# Relationship Notes — 2026-07-12

## 17:14
- W: convattic.com frontend is a prerendered React SPA (scripts/prerender.ts) deployed on Vercel; '/' serves dist/landing prerender, '/hu' serves dist/hu, catch-all serves dist/index.html shell
- Session: Fixed OG tags missing on convattic.com/ by converting vercel.json rewrites to legacy routes with handle:filesystem so the / -> /landing rewrite runs before the filesystem lookup that was shadowing it
